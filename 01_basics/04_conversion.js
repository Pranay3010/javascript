// // 01 ,02,03 are in Git hub repo of Hitesh Choudhary

// // console.log(2>1);  // ans= true(boolean)
// // console.log("2" > "1");  // ans= true(boolean)

// console.log("2">1);
// // when we compare string with number then js convert string into number and then compare
// // so here "2" is converted into 2 and then compared with 1
// // so ans is true(boolean)

// console.log(null > 0); // ans= false(boolean)
// // when we compare null with number then js convert null into 0 and then compare
// // so here null is converted into 0 and then compared with 0
// // so ans is false(boolean)

// console.log(null == 0); // ans= false(boolean)
// // when we compare null with number then js convert null into 0 and then compare
// // so here null is converted into 0 and then compared with 0
// // so ans is false(boolean)

// console.log(null >= 0); // ans= true(boolean)
// // when we compare null with number then js convert null into 0 and then compare
// // so here null is converted into 0 and then compared with 0
// // so ans is true(boolean)

// console.log(undefined==0);
// console.log(undefined>0);
// console.log(undefined >=0);


// === strict equality operator
// it does not do type coercion
// it checks both value and type

console.log(2 === 2);
console.log(2 === "2");
